{
  "name": "@asterdex/subgraph",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create --node http://localhost:8020/ asterdex/apex-protocol",
    "remove-local": "graph remove --node http://localhost:8020/ asterdex/apex-protocol",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 asterdex/apex-protocol",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ asterdex/apex-protocol",
    "deploy-hosted": "graph deploy --product hosted-service asterdex/apex-protocol"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.68.0",
    "@graphprotocol/graph-ts": "^0.32.0"
  }
}
